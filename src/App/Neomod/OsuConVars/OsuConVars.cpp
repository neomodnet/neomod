#include "OsuConVars.h"

#undef OSU_CONVARDEFS_H
#define DEFINE_OSU_CONVARS  // define to actually create the convars in this translation unit only (else they are just declared as extern)

#include "OsuConVarDefs.h"
